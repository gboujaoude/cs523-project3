// Creates no lymphocytes

+ world_width = 2000 // How long the world is
+ world_height = 7000 // How tall the world is

+ macrophage_num = 75
+ macrophage_speed = 45 // pixels per second - 3 * 15 (15 is the scaling factor)

+ virus_initial_num = 10
+ virus_speed = 45 // pixels per second - 3 * 15 (15 is the scaling factor)
+ virus_per_second = 0.5
+ viruses_before_explosion = 10

+ lymphocyte_initial_num = 0
+ lymphocyte_lifespan = 180 // seconds (regardless of current lymphocyte action)
+ lymphocyte_per_second = 4
+ lymphocyte_speed = 150 // pixels per second - 10 * 15 (second 15 is the scaling factor)

+ cytokine_speed = 150 // pixels per second - 10 * 15 (second 15 is the scaling factor)
+ cytokine_pouch_size = 0
+ cytokine_seconds_until_duplication = 6 // Seconds until a cytokine will create a copy of itself

+ livercell_initial_num = 2000 // Number of liver cells that get created at the beginning
+ max_runtime = 600 // max seconds allowed to runs

+ memo = MacrophageOnly // Written to sticky notes
+ config_classification = hypo4_onlyMacro10Viruses // Folder that tests will be thrown into
